# --- Generated data (regenerable by the pipeline) ---
datamart/

# --- Raw data: usually too large for GitHub. ---
# NOTE: the assignment SUBMISSION zip must still include data/ and datamart/.
# Keep them out of git, but add them back when you build the final zip.
data/*.csv

# --- Model bank: keep the artifact if small; comment out to track it ---
# model_bank/*.pkl

# --- Credentials / secrets ---
fernet_key.txt

# --- AI / Claude Code project files (local only, not for submission) ---
CLAUDE.md
.claude/

# --- Docs / walkthrough (kept on disk for submission zip; not for git) ---
WALKTHROUGH.md
Assignment 2.pdf

# --- Debug / scratch scripts (not part of the pipeline) ---
scripts/investigate.py
scripts/investigate_host.py
scripts/print_monitoring_table.py
scripts/verify_outputs.py

# --- Large notebooks (exploratory only; 21 MB) ---
notebooks/exploratory_data_analysis.ipynb

# --- Old A1 reference folders (data CSVs, classmate PDFs, duplicate code) ---
Data Pipeline/
ML Pipeline/

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.venv/
venv/
env/

# --- Spark / logs ---
spark-warehouse/
metastore_db/
derby.log
logs/
*.log

# --- Airflow local state ---
airflow.db
airflow.cfg
airflow_data/

# --- OS / editor ---
.DS_Store
Thumbs.db
.vscode/
.idea/
